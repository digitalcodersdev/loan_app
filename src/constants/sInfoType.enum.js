/*
 * Here we set the user related constant
 * @author Kindajobs <jdeveloper.vimal@gmail.com>
 */
const SInfoTypeEnum = {
  JWT: 'JWT',
  USER_CONTEXT: 'USER_CONTEXT',
};

export default Object.freeze(SInfoTypeEnum);
